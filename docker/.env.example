# CineVault v2 Configuration
APP_PORT=8080

# Database
DB_USER=cinevault
DB_PASS=cinevault
DB_NAME=cinevault

# Security
JWT_SECRET=change-me-to-a-random-string

# Media directory (read-only mount)
MEDIA_DIR=/mnt/media

# Cache Server (optional)
CACHE_SERVER_URL=
CACHE_SERVER_API_KEY=

# Hardware Acceleration: cpu, qsv, cuda
HW_ACCEL_TYPE=cpu
MAX_TRANSCODES=2
