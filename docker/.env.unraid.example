# CineVault – Unraid Environment Configuration
# Copy to .env in same directory and customize

# ── Database ──
DB_USER=cinevault
DB_PASSWORD=change-me-strong-password
DB_NAME=cinevault
DB_EXPOSE_PORT=5432

# ── Redis ──
REDIS_EXPOSE_PORT=6379

# ── Server ──
SERVER_PORT=8080
JWT_SECRET=change-me-in-production

# ── Media Path (host path on Unraid) ──
# Point this to wherever your media lives on Unraid
MEDIA_HOST_PATH=/mnt/user/media

# ── Metadata APIs ──
TMDB_API_KEY=
