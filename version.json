{
  "version": "0.59.00",
  "phase": 11,
  "released": "2026-02-12",
  "notes": "Phase 10+11: Integrations & Security — Trakt.tv OAuth2 device code auth with bidirectional scrobble, Last.fm scrobbling for music, Sonarr/Radarr/Lidarr webhook receiver with shared secrets, API key authentication (X-API-Key header, scoped permissions, create/revoke UI), OpenAPI 3.0 spec with Swagger UI at /api/docs, Plex/Jellyfin import tool framework, backup and restore (pg_dump, download, history), 2FA via TOTP (setup wizard, QR code, recovery codes, login validation), rate limiting with IP blocking (5 auth/min, 15min lockout), image optimization with WebP negotiation, JS module foundation (api.js, utils.js), PWA support (manifest.json, service worker, offline indicator). DB migrations 031+032."
}
